import { Box, Typography } from '@mui/material'

export default function About() {
	return (
		<Box id='About' sx={{ p: { xs: 2, sm: 3 } }}>
			<Typography variant='h1'>About</Typography>
		</Box>
	)
}
